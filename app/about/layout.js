export default function AboutLayout({ children }) {
    return (
        <>
            <h2>hi there this is the header inside the about layout</h2>
            <main>
                {children}
            </main>
        </>
    )
  }